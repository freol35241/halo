# Task 018 — PWA Configuration

**Status:** `[X]`

## Summary

Configure the SvelteKit app as a Progressive Web App. Manifest, service worker, icons, standalone display mode, home screen install prompt.

## Acceptance Criteria

- `manifest.json` with app name "HALO", theme color `#0f1117`, icons ✅
- Service worker for offline app shell caching ✅
- "Add to Home Screen" prompt logic ✅
- Standalone display mode (no browser chrome) ✅
- App icons generated (192x192, 512x512) ✅

## Review Result

**APPROVED** — Clean implementation hitting all acceptance criteria. Service worker strategy (cache-first static, network-first navigation, skip API/proxy) is well-considered. PWA store tests are thorough. Install prompt UX is polished with proper safe-area handling.

## Build Summary

### What was implemented

**Static assets (`static/`)**
- `static/manifest.json` — Web App Manifest with name "HALO", `display: "standalone"`, theme/background color `#0f1117`, and SVG icon entries at 192x192 and 512x512
- `static/icons/icon-192.svg` — SVG app icon (192×192) with HALO logo: concentric rings, center dot, glowing arc, "HALO" wordmark using design tokens (`#161821` bg, `#c49a6c` accent)
- `static/icons/icon-512.svg` — Same design scaled to 512×512 with `purpose: any maskable` for Android adaptive icons
- `static/sw.js` — Service worker with cache-first for static assets, network-first for navigation (offline fallback to cached shell), skips API/IDE/proxy routes

**App shell (`src/app.html`)**
- Added `<link rel="manifest">` pointing to `manifest.json`
- Added `theme-color` meta tag (`#0f1117`)
- Added Apple PWA meta tags (`apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`, `apple-mobile-web-app-title`)
- Added `apple-touch-icon` link
- Inline script at body end registers `/sw.js` on `window load`
- Updated viewport to include `viewport-fit=cover` for notched phones

**Install prompt (`src/lib/stores/pwa.ts` + `src/lib/components/InstallPrompt.svelte`)**
- `pwa.ts` — Svelte writable store for `BeforeInstallPromptEvent`, exports `canInstall` derived store, `setInstallPrompt`, `dismissInstallPrompt`, and async `triggerInstallPrompt`
- `InstallPrompt.svelte` — Bottom banner that listens for `beforeinstallprompt`, intercepts it, and shows "Install / Later" buttons; dismisses after user choice
- Wired into `src/routes/+layout.svelte`

**ESLint config** — Added `serviceworker` globals entry for `static/sw.js`

### Tests added
- `src/lib/stores/pwa.test.ts` — 8 unit tests covering store initialization, setInstallPrompt, dismissInstallPrompt, triggerInstallPrompt (accepted + dismissed paths), and no-op when no event

### All checks pass
- `npm run test` — 33 test files, 304 tests ✅
- `npm run check` — 0 errors ✅
- `npm run lint` — 0 errors ✅
- `npm run build` — successful ✅
